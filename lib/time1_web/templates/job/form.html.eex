<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :job_id %>
  <%= number_input f, :job_id %>
  <%= error_tag f, :job_id %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :desc %>
  <%= textarea f, :desc %>
  <%= error_tag f, :desc %>

  <%= label f, :budget %>
  <%= number_input f, :budget %>
  <%= error_tag f, :budget %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
